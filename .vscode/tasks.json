{
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "gcc",
            "args": [
                "-g",
                "-o",
                "${workspaceFolder}/bin/game.exe",
                "${workspaceFolder}/src/game.c",
                "-lkernel32",
                "-luser32",
                "-lgdi32",
                "-mwindows"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}
